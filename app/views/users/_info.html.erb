<table class='table'>
  <tr><%= attachment_image_tag(user, :profile_image, :fill, 100, 100, fallback: "no_image.jpg") %></tr>
  <tr>
	  <th>name :</th>
	  <th><%= user.name %></th>
  </tr>
</table>
<br></br>
<div class='row mt-3'>
    <i class= "fas fa-user-cog"></i><%= link_to 'Edit',edit_user_path(user),class: "btn btn-outline-secondary btn-block edit_user_#{user.id}" %>
</div>